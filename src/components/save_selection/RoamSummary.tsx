import React from "react";

interface Props {}

const RoamSummary: React.FC<Props> = ({}) => {
  return (
    <div>
      <span>
        Select this option to organize your PkRoam Boxes without connecting to a
        game save.
      </span>
    </div>
  );
};

export default RoamSummary;
